# Data (regenerated by data_prep.py; real corpus is gated on Hugging Face)
data/corpus/
data/officeqa_full.csv
data/*.parquet

# Results are regenerated by run.py; keep the committed copies under results/ only if you want them
# (left untracked by default so graders can reproduce)
# results/

# Python
__pycache__/
*.pyc
.venv/
venv/
.env

# Model / HF caches
.cache/
*.log
